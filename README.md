# yall-data-hasura
parler data ported to docker/hasura/postgres

## Prerequisites

docker

hasura cli

in the root folder run:

```docker-compose up -d```


```hasura migrate apply```

```hasura seeds apply```

seeds planted

ok ur rdy
